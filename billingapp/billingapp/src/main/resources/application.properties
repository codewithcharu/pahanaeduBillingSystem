# -----------------------------
#  DATABASE (Profiles)
# -----------------------------
# Default profile
spring.profiles.active=mysql

# Shared JPA/Thymeleaf/Swagger settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.mode=HTML
spring.thymeleaf.encoding=UTF-8
spring.thymeleaf.cache=false

springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html

# Data initialization enabled to create default roles
spring.sql.init.mode=always
spring.sql.init.continue-on-error=true

# -----------------------------
#  EMAIL CONFIGURATION
# -----------------------------
# Note: Gmail requires App Password, not regular password
# Go to Google Account > Security > 2-Step Verification > App passwords
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=pahanaedub@gmail.com
spring.mail.password=trgy mhtj qtvm sabu
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# Email functionality enabled
app.email.enabled=true

# Additional SMTP properties for better Gmail compatibility
spring.mail.properties.mail.smtp.ssl.protocols=TLSv1.2
spring.mail.properties.mail.debug=true



